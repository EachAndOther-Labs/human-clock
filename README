*Human clock*

Node js app built with sockets io, to show photos from instagram via their live api.

*TODO*
  * test on the Pi
  * configure 3 instances
  * build the UI/integrate with the demo
  * write some test cases
  * build an admin/monitor section of the app to change the hash tag and the feeds to the app